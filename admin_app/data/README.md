The CSV files in this folder are the bundled synthetic research/demo seed data and import/export format.

After first startup, the application uses the shared SQLite database at `data/meditrack.db` as its source of truth. The admin dashboard no longer writes patient uploads directly back into these CSV files.
